#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/chiseche-agro/node_modules/.pnpm/rolldown@1.0.2/node_modules/rolldown/node_modules:/chiseche-agro/node_modules/.pnpm/rolldown@1.0.2/node_modules:/chiseche-agro/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/chiseche-agro/node_modules/.pnpm/rolldown@1.0.2/node_modules/rolldown/node_modules:/chiseche-agro/node_modules/.pnpm/rolldown@1.0.2/node_modules:/chiseche-agro/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../rolldown/bin/cli.mjs" "$@"
else
  exec node  "$basedir/../../../rolldown/bin/cli.mjs" "$@"
fi
# cmd-shim-target=/chiseche-agro/node_modules/.pnpm/vite@8.0.14_@types+node@24.12.4_esbuild@0.27.0_jiti@1.21.7_tsx@4.21.0_yaml@2.9.0/node_modules/rolldown/bin/cli.mjs
